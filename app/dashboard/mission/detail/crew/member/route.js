import Ember from 'ember';

export default Ember.Route.extend({
  // model() {
  //   const id = this.paramsFor('dashboard.mission.detail').id;
  //
  //   return this.store.findRecord('member', id);
  // }
});
