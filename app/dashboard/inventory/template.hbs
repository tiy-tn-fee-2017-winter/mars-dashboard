
  {{!-- <div class="alert alert-danger alert-dismissible">
    <h4><i class="icon fa fa-ban"></i> Alert!!!</h4>
    <p>Inventory is getting low. Based on your current mission, you can eat 5 cherrios per day!</p>
  </div> --}}

  <h2>Inventory</h2>

  {{!-- // Progess Bars --}}
  <div class="box">
    <div class="box-header with-border">
      <h3>Number of Foods Left</h3>
      <p>Click on an option below to update the amount!</p>
    </div>
    <div class="box-body">
      {{#each model as |rationType|}}
        {{#link-to "dashboard.inventory.change" rationType}}
          <div class="item-info">
            <p>{{rationType.name}}</p>
            <div class="progress">
              <div class="progress-bar" role="progressbar" style="{{calc-percent rationType.quantity rationType.remaining}}" aria-valuenow="{{calc-percent rationType.quantity rationType.remaining}}"></div>
            </div>
          </div>
        {{/link-to}}
      {{/each}}

    </div>
  </div>
 {{outlet}}


  {{!-- // Form Card --}}
  {{!-- <div class="box">
    <div class="box-header">
      <h3 class="card-title">How Many Did You Eat Today?</h3>
    </div>
    <div class="box-body">
      <form>
        {{#each model as |rationType|}}

          <p>
            <label for=""># of {{rationType.name}} Used</label>
            <input type="text">
          </p>
        {{/each}}

        <p>
          <button>Submit</button>
        </p>
      </form>
    </div>
 </div> --}}
